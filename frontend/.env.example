# Vite exposes only VITE_* variables to client code

# Base URL where the FastAPI backend listens
# If you're using Vite's dev proxy for /api, you can still set this for explicit calls.
VITE_API_BASE=http://127.0.0.1:8000

# Google OAuth (frontend needs the Client ID for the Google button / flow)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
