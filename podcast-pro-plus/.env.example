# ==== FastAPI / App ====
APP_ENV=local
LOG_LEVEL=INFO
# Required for SessionMiddleware (Google OAuth). Use a strong random string.
SECRET_KEY=change-me-32-bytes-min

# Allow local Vite dev server
CORS_ALLOW_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# ==== Database (SQLite local) ====
# For SQLite file in this directory:
DATABASE_URL=sqlite:///./database.db

# ==== External services ====
# OpenAI (used for transcription)
OPENAI_API_KEY=sk-...

# ElevenLabs (used for TTS)
ELEVENLABS_API_KEY=...

# Path to ffmpeg binary (if not on PATH, give absolute path)
FFMPEG_PATH=ffmpeg

# ==== Google OAuth (auth router uses SessionMiddleware) ====
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
# Must match route mounted in FastAPI
GOOGLE_REDIRECT_URI=http://127.0.0.1:8000/api/auth/google/callback

# ==== Spreaker OAuth2 / API ====
SPREAKER_CLIENT_ID=your-spreaker-client-id
SPREAKER_CLIENT_SECRET=your-spreaker-client-secret
SPREAKER_REDIRECT_URI=http://127.0.0.1:8000/api/spreaker/auth/callback

# ==== Celery / RabbitMQ ====
RABBITMQ_URL=amqp://guest:guest@127.0.0.1:5672//
